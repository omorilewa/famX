type SignupUserPayload {
  id: ID!
  token: String!
}

extend type Mutation {
  signupUser(
    firstName: String!,
    lastName: String!,
    email: String!,
    password: String!,
    passwordConfirm: String!,
    phoneNum: String!): SignupUserPayload
}
